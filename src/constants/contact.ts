// Shared across Footer, Returns Policy, and anywhere else that needs the
// storefront's general contact email, so they can't drift out of sync.
export const SUPPORT_EMAIL = "sales@partshubaustralia.com.au";
